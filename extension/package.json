{
  "name": "atlas-time-travel",
  "displayName": "Atlas - Code Time Travel",
  "description": "Automatic code snapshots with time travel debugging",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "atlas.startWatching",
        "title": "Atlas: Start Watching Files"
      },
      {
        "command": "atlas.stopWatching",
        "title": "Atlas: Stop Watching Files"
      },
      {
        "command": "atlas.showStatus",
        "title": "Atlas: Show Status"
      },
      {
        "command": "atlas.listSnapshots",
        "title": "Atlas: List Snapshots"
      },
      {
        "command": "atlas.openTimeline",
        "title": "Atlas: Open Time Travel"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/diff": "^7.0.2",
    "@types/node": "20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "diff": "^8.0.2"
  }
}
